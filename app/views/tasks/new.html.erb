<h1>New Task</h1>

<%= form_with(model: [@project, @task], :url => { :action => :create }, local: true) do |form| %>
  <%= render 'form', task: @task, form: form %>
<% end %>

<%= link_to 'Back', project_tasks_path(@project) %> |
<%= link_to 'Projects', projects_path %>
